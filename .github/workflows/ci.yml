name: CI

on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - name: Install
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Unit tests
        run: npm run test

      - name: Build
        run: npm run build
        env:
          NEXT_PUBLIC_STORAGE_SUPABASE_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_STORAGE_SUPABASE_SUPABASE_ANON_KEY: placeholder-anon-key

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: E2E smoke tests
        run: npm run test:e2e
        env:
          NEXT_PUBLIC_STORAGE_SUPABASE_SUPABASE_URL: https://placeholder.supabase.co
          NEXT_PUBLIC_STORAGE_SUPABASE_SUPABASE_ANON_KEY: placeholder-anon-key
