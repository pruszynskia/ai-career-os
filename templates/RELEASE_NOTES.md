# `<vX.Y.Z>` — `<YYYY-MM-DD>`

## Added

- `<new feature>`

## Changed

- `<behavior change>`

## Fixed

- `<bug fix>`

## Removed

- `<deprecated/removed capability>`

## Upgrade Notes

`<Anything a user/developer must do when moving to this version — env vars,
migrations, breaking API changes. Omit this section if there's nothing.>`
