# Project Planning: `<Project Name>`

## Problem

`<What problem exists today, for whom>`

## Goal

`<What success looks like, in one sentence>`

## Non-Goals

- `<explicitly out of scope for this planning cycle>`

## Constraints

- `<team size, timeline, budget, technical constraint>`

## Approach

`<High-level shape of the solution — not implementation detail>`

## Milestones

| Milestone | Outcome                           | Target            |
| --------- | --------------------------------- | ----------------- |
| `<M1>`    | `<what's true when this is done>` | `<date/estimate>` |

## Open Questions

- `<question that needs an answer before/during implementation>`

## Next Step

Turn this into `docs/PRODUCT.md` (if new product) or backlog tasks in
`backlog/mvp.yaml` (if scoped work on an existing product) — see
`WORKFLOW.md`.
